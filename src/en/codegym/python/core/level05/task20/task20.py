# Unpacking

# Write a program that creates a tuple of 3 elements (e.g., name, age, city).
# Then the program should unpack the tuple into separate variables and print each variable.

my_tuple = ("John", 25, "New York")
name, age, city = my_tuple
print(name, age, city)
