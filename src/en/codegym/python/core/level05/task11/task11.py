# Forest

# Write a program that creates a list of tree names, then uses a loop and the enumerate() function to display
# each element of the list and its index.

tree_list = ["artar", "stejar", "mesteacan", "plop", "frasin"]
for index, element in enumerate(tree_list):
    print(f" At index {index} we have {element} tree ")

