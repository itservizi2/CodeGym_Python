# The Indenter
# Fix the indentations in the following code so that it correctly asks
# for the user's name and outputs a greeting message.

name = input("Enter your name: ")
if name:
    print("Hello, " + name + "!")
else:
    print("You didn't enter a name.")
