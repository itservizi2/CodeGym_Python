# Creating sets

# Write a program that creates 5 sets:
# a set with 0 elements,
# a set with 1 element,
# a set with 5 elements,
# a set with 100 elements,
# a set with 1000 elements

my_set0 = set()
my_set1 = {1}
my_set2 = {1,2,3,4,5}
my_set100 = {i for i in range(100)}
my_set1000 = {i for i in range(1000)}
